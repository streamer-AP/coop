import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const primary = Color(0xFF6750A4); // TODO: use actual brand color
  static const secondary = Color(0xFF625B71);
  static const error = Color(0xFFB3261E);
}
