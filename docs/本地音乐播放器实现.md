# Flutter 本地音乐播放器实现

## 已实现功能

### 1. 核心播放功能
- ✅ 音频播放服务 (`AudioPlayerService`)
  - 基于 `just_audio` + `audio_service`
  - 支持后台播放
  - 锁屏/通知栏控制
  - 音频焦点管理

### 2. 歌词支持
- ✅ 多格式歌词解析 (`SubtitleService`)
  - SRT (SubRip)
  - VTT (WebVTT)
  - LRC (歌词)
  - SUB (MicroDVD)
  - STL (Spruce)
- ✅ 歌词同步显示 (`SubtitleView`)
  - 自动滚动
  - 高亮当前行
  - 点击跳转

### 3. 本地音乐扫描
- ✅ 设备音乐库扫描 (`LocalMusicScannerService`)
  - 基于 `on_audio_query`
  - 自动权限请求
  - 封面提取
  - 按专辑/艺术家分组

### 4. UI 组件
- ✅ 播放器控制 (`PlayerControls`)
- ✅ 进度条 (`SeekBar`)
- ✅ 迷你播放条 (`MiniPlayerBar`)
- ✅ 歌词视图 (`SubtitleView`)
- ✅ 本地音乐列表 (`LocalMusicScreen`)

## 使用示例

### 初始化播放器

```dart
final playerService = AudioPlayerService();
await playerService.initialize();
```

### 扫描本地音乐

```dart
// 在 Widget 中使用
class MyWidget extends ConsumerWidget {
  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final musicList = ref.watch(localMusicListProvider);

    return musicList.when(
      data: (songs) => ListView.builder(
        itemCount: songs.length,
        itemBuilder: (context, index) {
          return ListTile(
            title: Text(songs[index].title),
            onTap: () {
              // 播放歌曲
            },
          );
        },
      ),
      loading: () => CircularProgressIndicator(),
      error: (error, stack) => Text('Error: $error'),
    );
  }
}

// 触发扫描
ref.read(localMusicListProvider.notifier).scan();
```

### 播放音乐

```dart
// 设置音频源
await playerService.setSource(audioEntry);

// 播放
await playerService.play();

// 暂停
await playerService.pause();

// 跳转
await playerService.seekTo(Duration(seconds: 30));
```

### 加载歌词

```dart
final subtitleService = SubtitleService();

// 解析 LRC 文件
final lrcContent = await File('song.lrc').readAsString();
final subtitle = subtitleService.parse(
  SubtitleRef(
    id: 1,
    entryId: audioEntry.id,
    language: 'zh',
    filePath: 'song.lrc',
    format: SubtitleFormat.lrc,
  ),
  lrcContent,
);

// 获取当前位置的歌词
final cue = subtitleService.cueAtPosition(subtitle, currentPosition);
print(cue?.text);
```

## 权限配置

### Android

在 `android/app/src/main/AndroidManifest.xml` 添加：

```xml
<uses-permission android:name="android.permission.READ_MEDIA_AUDIO"/>
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>
```

### iOS

在 `ios/Runner/Info.plist` 添加：

```xml
<key>NSAppleMusicUsageDescription</key>
<string>需要访问您的音乐库以播放本地音乐</string>

<key>UIBackgroundModes</key>
<array>
    <string>audio</string>
</array>
```

详细配置见 `docs/本地音乐权限配置.md`

## 依赖

```yaml
dependencies:
  just_audio: ^0.9.43
  audio_service: ^0.18.17
  audio_session: ^0.1.21
  on_audio_query: ^2.9.0
  permission_handler: ^11.3.1
  flutter_lyric: ^2.0.4+6  # 可选，用于更高级的歌词显示
```

## 下一步

- [ ] 播放列表管理
- [ ] 播放模式（顺序/随机/单曲循环）
- [ ] 均衡器
- [ ] 睡眠定时器
- [ ] 歌词编辑
- [ ] 在线歌词搜索
